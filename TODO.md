TODOs
========

* ! invent own generic yaml DSL
* Kaml.K8s
    - metadata namespace
    - replica set reuses pod spec (template)
    - Deployment, Service,... (provide a lot examples with explanatory comments)
* Kaml.Github (workflow action)
* Kaml.OpenApi
* Kaml.AzureBuild (pipeline job),
* Kaml.Kustomize.* (base, overlays, ... all in one file. it will setup dir structure for you)
* github pages

Ad Github Actions

* multi-line strings (preserved | and folded >)
* extensibility: allow for totally custom yaml entries
* support comments
* showcase building layer on top of DSL (reuse, reference, ...)
* generation modes: 1) inline 2) reuse/reference (the typical way when handwriting them)